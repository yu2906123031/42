# 42 自动买入策略

## 1. 策略目标

该策略用于 `42.space` 的 Futures Daily Volume 市场，核心逻辑是：

```text
发现明日市场 → UTC 新日开始后读取 Binance Futures 日成交额 → 预测最终 Daily Vol → 匹配 outcome 区间 → 通过过滤后自动买入
```

当前支持交易对：

- `BNB/USDT`：默认买入 `5 USDT`
- `BTC/USDT`：默认买入 `2 USDT`
- `SOL/USDT`：默认买入 `2 USDT`
- `ETH/USDT`：默认买入 `2 USDT`

数据源统一使用 Binance Futures 永续合约 K 线里的 `quoteAssetVolume`，即 USDT 计价成交额。

## 2. 时间规则

```text
北京时间 07:59:30：预扫描下一 UTC 日市场
北京时间 08:00:00 后：开始预测新 UTC 日成交额
过滤通过：自动提交买入
同一北京时间日期：最多启动一组自动买入
```

关键点：

- 07:59:30 只发现市场地址。
- 08:00:00 前不会用旧 UTC 日成交额为新市场下单。
- 自动买入启动后会写入本地状态，防止同日重复下单。

## 3. 成交额预测模型

实时读取：

```text
当前 UTC Daily Vol(USDT)
最近 15 分钟成交速度
最近 1 小时成交速度
最近 4 小时成交速度
最近 12 小时成交速度
当前 UTC 日平均速度
最近 7 日同时间段历史速度
```

常规预测公式：

```text
预计剩余成交速度 = 最近 1 小时速度 * 50%
              + 最近 4 小时速度 * 30%
              + 当前日平均速度 * 20%

预测最终成交额 = 当前成交额 + 预计剩余成交速度 * 剩余小时数
```

预测范围：

```text
下界 = 当前成交额 + 最低参考速度 * 剩余小时数
上界 = 当前成交额 + 最高参考速度 * 剩余小时数
```

参考速度包括：`1h`、`4h`、`12h`、当前日均速，以及可用时的历史同时间段速度。

## 4. Opening Snipe 模式

默认开启：

```env
OPENING_SNIPE_MODE=true
OPENING_SNIPE_WINDOW_MINUTES=30
```

UTC 新日开始后的前 30 分钟，成交额样本偏少，策略使用历史均值预测：

```text
预测最终成交额 = 7日均值 * 50% + 30日均值 * 30% + 3日均值 * 20%
```

Opening Snipe 生效时：

- 使用历史成交额预测。
- 跳过 `Daily Volume Below Limit` 过滤。
- 仍然检查过高成交额、置信度、价格上限。

## 5. 区间选择

程序从 42 市场 outcomes 动态解析区间，例如：

```text
0M-150M
150M-300M
300M-450M
450M-600M
600M+
```

选择规则：

- 预测最终成交额落在哪个区间，就买哪个区间。
- 区间边界按左闭右开处理：`300M` 归入 `300M-450M`。
- 置信度按预测范围与目标区间的重叠比例计算，结果限制在 `5%~95%`。

## 6. 买入过滤

自动买入前必须通过以下过滤：

- 当前 Daily Vol >= `AUTO_SKIP_IF_DAILY_VOL_BELOW`
  - 默认：`100000000`
  - Opening Snipe 模式会跳过该过滤
- 当前 Daily Vol <= `AUTO_SKIP_IF_DAILY_VOL_ABOVE`
  - 默认：`1000000000`
- 预测区间置信度 >= `MIN_CONFIDENCE`
  - 默认：`60`
- outcome 当前价格 <= `AUTO_MAX_OUTCOME_PRICE`
  - 默认：`0.45`
- 同一北京时间日期尚未启动过自动买入
- Router 链上模拟成功
- USDT 余额足够

放弃原因会写入本地预测历史，例如：

```text
Daily Volume Below Limit
Daily Volume Above Limit
Low Confidence
Outcome Overpriced
No Predicted Outcome
No Qualified Outcome
```

## 7. 下单规则

默认 SMART 下单：

```text
每个启用交易对只买预测命中的主区间
副仓默认关闭
不自动卖出
```

当前代码里 SMART 自动买入调用参数为：

```text
include_secondary=False
```

所以即使配置了 `SECONDARY_BUY_USDT`，自动买入流程也只提交主区间。

## 8. 固定阶梯模式

桌面端有一个可选开关：

```text
固定阶梯：300M-450M 买入 5 USDT
```

启用后，仅对 `BNB/USDT` 生效：

- 固定买 `300M-450M` 区间。
- 仍然检查置信度、成交额上限等过滤。
- 价格检查在固定阶梯下由固定阶梯逻辑内部处理。

该模式适合明确只想押一个固定区间时使用。

## 9. 授权与链上执行

链上执行流程：

1. 并发探测 BSC RPC，选择可用最快节点。
2. 检查目标 outcome 与实时价格。
3. 检查账户 USDT 余额。
4. 只授权当前订单所需 USDT。
5. 使用 Lens / Router 模拟交易。
6. 根据滑点设置计算最低 OT 输出保护。
7. 广播交易。
8. 将实际成交写入 `runtime-state/trades.db`。

默认滑点：

```env
SLIPPAGE_BPS=200
```

含义：约 `2%` 输出保护。

## 10. 本地数据

成交与状态：

```text
runtime-state/trades.db
```

成交额历史与预测：

```text
runtime-state/volume_history.db
runtime-state/volume_history_btcusdt.db
runtime-state/volume_history_solusdt.db
runtime-state/volume_history_ethusdt.db
```

主要表：

- `volume_history`：历史 UTC 日成交额
- `volume_predictions`：预测值、目标区间、价格、决策、原因
- `prediction_accuracy`：预测误差与区间命中率

## 11. 当前推荐配置

```env
AUTO_MODE=SMART
PRIMARY_BUY_USDT=5
SECONDARY_BUY_USDT=0
AUTO_MAX_OUTCOME_PRICE=0.45
MIN_CONFIDENCE=60
AUTO_SKIP_IF_DAILY_VOL_BELOW=100000000
AUTO_SKIP_IF_DAILY_VOL_ABOVE=1000000000
OPENING_SNIPE_MODE=true
OPENING_SNIPE_WINDOW_MINUTES=30
AUTO_PREDICT_INTERVAL_SECONDS=60
SLIPPAGE_BPS=200
```

## 12. 风控建议

当前策略偏向开盘抢先预测，核心风险在前 30 分钟历史均值预测失真。

建议默认保持：

- `SECONDARY_BUY_USDT=0`
- `AUTO_MAX_OUTCOME_PRICE=0.45`
- `MIN_CONFIDENCE=60`
- 单交易对小金额运行
- 先观察 `volume_predictions` 的命中率，再提高金额

如果市场刚开盘价格已经高于 `0.45`，让策略自动放弃。