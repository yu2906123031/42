# Smart Daily Volume Trader V2

## 策略目标

本策略用于 `42.space` 的 `BNB/USDT Futures Daily Volume` 市场，从固定区间买入升级为成交量预测驱动的动态选区间系统。

| 项目 | 规则 |
| --- | --- |
| 数据源 | Binance Futures `BNBUSDT` Perpetual |
| 统计指标 | UTC 自然日 `1D Candle` 的 `Vol(USDT)` / `quoteAssetVolume` |
| 主仓 | 自动预测区间，BNB 默认 `5 USDT`，BTC/SOL/ETH 默认 `2 USDT` |
| 副仓 | 默认关闭，不再额外买入相邻区间 |
| 卖出 | 不自动卖出 |

禁止使用滚动 24 小时 `quoteVolume`、现货成交额或 `Vol(BNB)` 作为预测和结算判断依据。

## 执行时间

```text
北京时间 07:59:30：预扫描即将开始的 UTC 新日市场
北京时间 08:00:00 之后：开始使用新 UTC 日 Daily Vol 运行预测和过滤
过滤通过：提交动态选择的主仓买入；成功后可提交副仓
```

`07:59:30` 时当前日 K 线仍属于前一个 UTC 日，因此程序不会使用旧日成交额为新市场下单。

## 预测模型

实时计算：

```text
当前 UTC Daily Vol(USDT)
最近 15 分钟成交速度
最近 1 小时成交速度
最近 4 小时成交速度
当前 UTC 日平均速度
```

加权预测：

```text
预计剩余成交速度 =
  最近 1 小时速度 * 50%
  + 最近 4 小时速度 * 30%
  + 当前日平均速度 * 20%

预测最终成交额 =
  当前成交额 + 预计剩余成交速度 * 剩余小时数
```

软件从市场所有 outcomes 动态读取区间，例如：

```text
0-150M
150-300M
300-450M
450-600M
600M+
```

预测值自动匹配所属区间。边界按左闭右开处理，例如 `300M` 归入 `300M-450M`。

## 历史数据

程序拉取并保存 BNB 最近 30 个已收盘 UTC 日成交额到：

```text
runtime-state/volume_history.db
```

表：

| 表名 | 用途 |
| --- | --- |
| `volume_history` | `date`、`volume_usdt`、`day_of_week` 历史数据 |
| `volume_predictions` | 每次预测、选区间、价格、决策和放弃原因 |
| `prediction_accuracy` | 后续回测预测误差与区间命中率 |

统计指标包括：

```text
7日平均、30日平均
7日中位数、30日中位数
30日标准差、最大值、最小值
最近3日平均
```

## 自动买入过滤

买入前必须全部通过：

| 条件 | 默认值 | 未通过记录 |
| --- | ---: | --- |
| 当前 Daily Vol 不过低 | `>= 100,000,000` | `Daily Volume Below Limit` |
| 当前 Daily Vol 不过高 | `<= 1,000,000,000` | `Daily Volume Above Limit` |
| 预测区间置信度 | `>= 60%` | `Low Confidence` |
| 主区间 outcome 价格 | `<= 0.45` | `Outcome Overpriced` |
| Router 链上模拟 | 成功 | 不广播交易 |
| 同日任务 | 未执行过 | 不重复买入 |

主区间通过过滤后，程序自动选择最接近预测值的另一区间作为副仓候选；副仓价格超过上限时不提交副仓。

## 下单和授权

1. 并发探测官方 BSC RPC，使用可用最快节点并保留回退节点。
2. 检查目标 outcome 与实时价格。
3. 检查账户 USDT 余额。
4. 仅授权当前订单所需 USDT，不使用无限授权。
5. 使用 Lens 模拟并计算 Router 最低 OT 输出保护。
6. 广播交易并按链上实际成交额写入历史记录。

```env
SLIPPAGE_BPS=200
```

默认输出滑点保护约为 `2%`。

## Dashboard 显示

`Daily Volume Predictor` 显示：

```text
当前 Daily Vol
最近 15 分钟 / 1 小时 / 4 小时速度
预计最终成交额
预测范围
预测区间
置信度
最近 7 天均值
最近 30 天均值
目标区间
当前价格
判断状态
```

`BNB / BTC / SOL / ETH 合约日成交额历史` 为折叠区域，展开后从 Binance Futures 公共日线加载明细。

## 配置

```env
AUTO_MODE=SMART
PRIMARY_BUY_USDT=5
SECONDARY_BUY_USDT=0
AUTO_MAX_OUTCOME_PRICE=0.45
MIN_CONFIDENCE=60
AUTO_SKIP_IF_DAILY_VOL_BELOW=100000000
AUTO_SKIP_IF_DAILY_VOL_ABOVE=1000000000
OPENING_SNIPE_MODE=true
OPENING_SNIPE_WINDOW_MINUTES=30
AUTO_PREDICT_INTERVAL_SECONDS=60
SLIPPAGE_BPS=200
```

## 持仓规则

```text
只自动买入，不自动卖出。
```

软件不会自动平仓、止盈、止损、卖出 OT 或赎回结算收益。
